{
  "motion": {
    "transition": {
      "fast": { "$value": "150ms ease", "$type": "string", "$description": "Snap interactions (button hover, focus ring)" },
      "base": { "$value": "250ms ease", "$type": "string", "$description": "Default UI transitions (accordions, dropdowns)" },
      "slow": { "$value": "400ms ease", "$type": "string", "$description": "Page-level fades and large surface motion" }
    }
  }
}
