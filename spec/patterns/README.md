# Layout patterns (prototype)

Composite layouts on `/patterns` in the Next.js prototype. Each pattern combines `.fe-*` components and visual assets documented in [docs/visual-styles.md](../../docs/visual-styles.md).

| Section ID | Pattern | Key classes / assets |
|---|---|---|
| `#header-pattern` | Header (desktop & mobile) | `.fe-header`, `HeaderDemo` |
| `#footer-pattern` | Footer | `.fe-footer`, link columns, newsletter, social icons |
| `#category-filter` | Category filter bar | `.fe-tag-pill--*` |
| `#hero-pattern` | Hero with blob photo | `.ds-hero-with-image`, `.ds-headline-underline`, hero photo |
| `#activity-workshop-card` | Activity workshop card | `.fe-card`, `.fe-card-category` |
| `#workshop-card` | Workshop card (full) | `.fe-card`, `.fe-card-category` |
| `#events` | Upcoming workshops grid | `.fe-event-tabs`, `.ds-events-grid` |
| `#benefit-cards` | Benefit & get involved | `.fe-card-benefit`, `.fe-card-get-involved`, line illustrations |

Elementor: map patterns to sections/widgets manually; see [elementor/custom-css-setup.md](../../elementor/custom-css-setup.md).
